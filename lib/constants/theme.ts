import { colors } from '@/lib/styles/colors'

/** JS-side accent for `${COCKPIT_ACCENT}33` opacity tricks. For plain CSS
 *  use `var(--color-cockpit-accent)` directly. */
export const COCKPIT_ACCENT = colors['cockpit-accent']
